function initWormholeCanvas(){const canvas=document.getElementById('bg3d');if(!canvas)return;const ctx=canvas.getContext('2d');canvas.width=window.innerWidth;canvas.height=window.innerHeight;let time=0;const particlesArray=[];class Particle{constructor(){this.x=Math.random()*canvas.width;this.y=Math.random()*canvas.height;this.size=Math.random()*2+1;this.opacity=Math.random()*.5+.5;this.speedX=(Math.random()-.5)*2;this.speedY=(Math.random()-.5)*2}draw(){ctx.save();ctx.globalAlpha=this.opacity;ctx.fillStyle='rgba(167,139,250,1)';ctx.fillRect(this.x,this.y,this.size,this.size);ctx.restore()}update(){this.x+=this.speedX;this.y+=this.speedY;if(this.x<0||this.x>canvas.width)this.speedX*=-1;if(this.y<0||this.y>canvas.height)this.speedY*=-1;this.opacity+=Math.sin(time*.01)*.01;if(this.opacity>1)this.opacity=1;if(this.opacity<.3)this.opacity=.3}}function initParticles(){for(let i=0;i<30;i++){particlesArray.push(new Particle())}}function drawWormhole(){ctx.fillStyle='#000814';ctx.fillRect(0,0,canvas.width,canvas.height);const centerX=canvas.width/2;const centerY=canvas.height/2;const rings=35;for(let i=rings;i>0;i--){const radius=(canvas.height/rings)*i;const hue=(time*2.5+i*12)%360;const brightness=60+Math.sin(time/8+i)*25;ctx.strokeStyle=`hsl(${hue},95%,${brightness}%)`;ctx.lineWidth=2.8;ctx.beginPath();ctx.arc(centerX,centerY,radius,0,Math.PI*2);ctx.stroke();const glowEffect=Math.sin(time/10+i)*20;ctx.shadowColor=`hsla(${hue},100%,${brightness}%,0.6)`;ctx.shadowBlur=glowEffect+10;ctx.strokeStyle=`hsla(${hue},90%,${brightness}%,0.3)`;ctx.lineWidth=4;ctx.stroke();ctx.shadowBlur=0}drawWaves()}function drawWaves(){const waveHeight=30;const frequency=.009;for(let y=0;y<canvas.height;y+=45){ctx.strokeStyle=`rgba(167,139,250,${.5+Math.sin(time/120+y/200)*.25})`;ctx.lineWidth=2.2;ctx.beginPath();for(let x=0;x<=canvas.width;x+=6){const offsetY=Math.sin(x*frequency+time/18)*waveHeight;ctx.lineTo(x,y+offsetY)}ctx.stroke()}for(const particle of particlesArray){particle.update();particle.draw()}}function animate(){drawWormhole();time++;requestAnimationFrame(animate)}initParticles();animate();window.addEventListener('resize',()=>{canvas.width=window.innerWidth;canvas.height=window.innerHeight})}const navLinks=document.querySelectorAll('a[href^="#"]');navLinks.forEach(anchor=>{anchor.addEventListener('click',function(e){e.preventDefault();const targetId=this.getAttribute('href');const target=document.querySelector(targetId);if(target){const headerOffset=80;const elementPosition=target.getBoundingClientRect().top+window.pageYOffset;const offsetPosition=elementPosition-headerOffset;window.scrollTo({top:offsetPosition,behavior:'smooth'})}})});window.addEventListener('scroll',function(){const navbar=document.querySelector('.navbar');if(!navbar)return;const scrollY=window.scrollY;if(scrollY>50){navbar.style.background='rgba(0,8,20,0.95)';navbar.style.backdropFilter='blur(12px)';navbar.style.boxShadow='0 4px 20px rgba(0,0,0,0.5)'}else{navbar.style.background='rgba(15,23,42,0.95)';navbar.style.backdropFilter='blur(10px)';navbar.style.boxShadow='none'}});const observerOptions={threshold:.1,rootMargin:'0px 0px -100px 0px'};const observer=new IntersectionObserver(function(entries){entries.forEach(entry=>{if(entry.isIntersecting){entry.target.style.animation='fadeInUp 0.6s ease forwards';observer.unobserve(entry.target)}})},observerOptions);document.querySelectorAll('.project-card').forEach(card=>{card.style.opacity='0';observer.observe(card)});document.querySelectorAll('.skill-category').forEach(card=>{card.style.opacity='0';observer.observe(card)});document.querySelectorAll('.timeline-content').forEach(card=>{card.style.opacity='0';observer.observe(card)});const style=document.createElement('style');style.textContent=`@keyframes fadeInUp{from{opacity:0;transform:translateY(20px)}to{opacity:1;transform:translateY(0)}}`;document.head.appendChild(style);window.addEventListener('load',()=>{const elements=document.querySelectorAll('.project-card, .skill-category, .timeline-content');elements.forEach((el,index)=>{el.style.animationDelay=`${index*100}ms`})});const projectLinks=document.querySelectorAll('.project-link');projectLinks.forEach(link=>{link.addEventListener('mouseenter',function(){this.style.transform='translateX(15px)';this.style.color='#f5f3ff'});link.addEventListener('mouseleave',function(){this.style.transform='translateX(0)';this.style.color='#c4b5fd'})});console.log('Portfolio loaded successfully! Welcome to Barath\'s professional portfolio.')
