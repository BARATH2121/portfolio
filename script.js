function initWormholeCanvas(){const canvas=document.getElementById('bg3d');if(!canvas)return;const ctx=canvas.getContext('2d');canvas.width=window.innerWidth;canvas.height=window.innerHeight;let time=0;function drawWormhole(){ctx.fillStyle='#000814';ctx.fillRect(0,0,canvas.width,canvas.height);const centerX=canvas.width/2;const centerY=canvas.height/2;const rings=30;for(let i=rings;i>0;i--){const radius=(canvas.height/rings)*i;const hue=(time*2+i*10)%360;const brightness=50+Math.sin(time/10+i)*20;ctx.strokeStyle=`hsl(${hue},90%,${brightness}%)`;ctx.lineWidth=2.5;ctx.beginPath();ctx.arc(centerX,centerY,radius,0,Math.PI*2);ctx.stroke()}drawWaves()}function drawWaves(){const waveHeight=25;const frequency=0.008;for(let y=0;y<canvas.height;y+=50){ctx.strokeStyle=`rgba(0,0,0,${0.6+Math.sin(time/100+y/200)*0.2})`;ctx.lineWidth=2;ctx.beginPath();for(let x=0;x<=canvas.width;x+=5){const offsetY=Math.sin(x*frequency+time/20)*waveHeight;ctx.lineTo(x,y+offsetY)}ctx.stroke()}}function animate(){drawWormhole();time++;requestAnimationFrame(animate)}animate();window.addEventListener('resize',()=>{canvas.width=window.innerWidth;canvas.height=window.innerHeight})}initWormholeCanvas();document.querySelectorAll('a[href^="#"]').forEach(anchor=>{anchor.addEventListener('click',function(e){e.preventDefault();const target=document.querySelector(this.getAttribute('href'));if(target){target.scrollIntoView({behavior:'smooth',block:'start'})}})});window.addEventListener('scroll',function(){const navbar=document.querySelector('.navbar');if(window.scrollY>50){navbar.style.background='rgba(0,8,20,0.9)'}else{navbar.style.background='rgba(12,45,87,0.8)'}});const observerOptions={threshold:0.1,rootMargin:'0px 0px -100px 0px'};const observer=new IntersectionObserver(function(entries){entries.forEach(entry=>{if(entry.isIntersecting){entry.target.style.animation='fadeInUp 0.6s ease forwards';observer.unobserve(entry.target)}})},observerOptions);document.querySelectorAll('.glass-card').forEach(card=>{card.style.opacity='0';observer.observe(card)});document.querySelectorAll('.project-card').forEach(card=>{card.style.opacity='0';observer.observe(card)});document.querySelectorAll('.skill-card').forEach(card=>{card.style.opacity='0';observer.observe(card)})
